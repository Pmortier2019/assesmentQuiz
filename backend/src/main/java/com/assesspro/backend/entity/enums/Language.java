package com.assesspro.backend.entity.enums;

/**
 * Supported languages. Currently the platform serves EN only.
 * NL, DE, FR, ES are prepared for future multilingual rollout.
 */
public enum Language {
    EN,
    NL,
    DE,
    FR,
    ES
}
